import styled from "styled-components";

// 다님 검색하기 텍스트
const DanimSearchText = styled.p`
  text-align: left;
  margin-bottom: 10px;
  font-weight: 400;
  & > span {
    font-weight: 600;
  }
`;

export default {
  DanimSearchText,
};
